/**
 * HCS-U7 Widget v3 — Allow action
 * Default UX = invisible. Nothing happens.
 */

import { log } from '../core/logger';

export function executeAllow(): void {
  log('action', 'ALLOW — no friction');
}
